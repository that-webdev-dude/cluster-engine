import * as Cluster from "../../../cluster";
import * as Components from "../components";
import * as Constants from "../constants";

export class AnyEntity extends Cluster.Entity {
  constructor() {
    super();
  }
}
