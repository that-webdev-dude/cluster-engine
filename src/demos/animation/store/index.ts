import * as Cluster from "../../../cluster";
import * as Events from "../events";

type State = {};

const state: State = {};

const mutations = {};

const actions = {};

const getters = {};

export const store = new Cluster.Store({
  state,
  mutations,
  actions,
  getters,
});
