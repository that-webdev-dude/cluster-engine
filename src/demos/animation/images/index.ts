import charactersImageURL from "../../../images/characters.png";
import weaponImageURL from "../../../images/weapon.png";
import * as Cluster from "../../../cluster";

const charactersImage = Cluster.Assets.image(charactersImageURL);

const weaponImage = Cluster.Assets.image(weaponImageURL);

export { charactersImage, weaponImage };
