import * as Images from "../../../images";
import * as Cluster from "../../../cluster";
import * as Components from "../components";
import { COLLISION_LAYERS } from "../constants";

export class TemplateEntity extends Cluster.Entity {
  constructor() {
    super();
  }
}
